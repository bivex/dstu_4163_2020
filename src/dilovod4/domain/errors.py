"""Доменні помилки.

Розмежування: DomainError — порушення інваріанта (структурна неможливість
існування документа), а НЕ невідповідність нормі. Невідповідність нормі
повертається як Finding у звіті й винятком не є.
"""

from __future__ import annotations


class DomainError(Exception):
    """Базова доменна помилка (бізнес-рівень)."""


class InvariantViolation(DomainError):
    """Порушено інваріант сутності/value object (напр. відʼємна кількість)."""
