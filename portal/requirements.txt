# Портал підписання та редагування документів — залежності
# Бекенд: FastAPI поверх доменного ядра dilovod4 (ДСТУ 4163 + 7 НПА).
# Підпис КЕП — на клієнті (EUSign, submodule external/EUSignES6); сервер
# приймає готові p7s/asice. Серверний підпис токеном/печаткою — через
# dilovod4.infrastructure.token_sign/asic (необовʼязково, для печатки юрособи).

fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
python-multipart>=0.0.9

# доменне ядро (генерація/валідація/КЕП-відмітки)
python-docx>=1.1
reportlab>=4.0
svglib>=1.5   # SVG → ReportLab Drawing: для векторного Герба України на бланках
segno>=1.6
pypdf>=4.0

# auth
PyJWT>=2.8

# тести (TestClient потребує httpx)
pytest>=8.0
httpx>=0.27
